name: ci-wf
on:
  pull_request:
    branches:
      - develop
  push:
    branches:
      - develop 
jobs:
  linting_job:
    runs-on: ubuntu-latest
    steps:
      - name: step1 of linting-job
        run: echo "Allah always help me"
  build-job:
    needs: "linting-job"
    runs-on: ubuntu-latest
    steps: 
      - name: Step 1 of build job
        run: echo "Build job"